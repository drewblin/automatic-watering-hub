# Light sleep контролера

## Цільовий варіант

- Додати планувальник найближчої події:
  - наступне читання лічильника води;
  - наступне читання датчика тиску;
  - наступне читання датчиків вологості;
  - час закриття відкритого клапана;
  - retry-delay для зони;
  - службові таймаути Wi-Fi/BLE/OTA.
- Якщо немає відкритого клапана, активного Wi-Fi API, OTA або BLE-з'єднання,
  переходити в `esp_light_sleep_start()`.
- Перед sleep налаштовувати wake-up джерело через `esp_sleep_enable_*`, мінімум
  timer wake-up до найближчої запланованої події.
- Після `esp_light_sleep_start()` виконання продовжується з наступного рядка, без
  reboot, зі збереженням RAM і стану задач.

## Передумови

- Додати фізичну кнопку або інший надійний input для config/wake режиму BLE.
- Після цього повністю вимикати BLE advertising поза BLE config mode, інакше BLE
  має залишатися доступним та обмежує глибину економії.
- Визначити політику Wi-Fi: залишатися online через modem power save або
  зупиняти Wi-Fi перед sleep і піднімати його лише у вікна синхронізації/API.
- Перевірити, як PCNT поводиться в light sleep для поточного ESP32 і обраної
  версії ESP-IDF/Arduino core.
- Якщо PCNT не рахує імпульси в потрібному sleep-режимі, перейти на wake-up від
  GPIO або іншу схему, щоб не пропускати лічильник води.

## Аргументація

- `esp_light_sleep_start()` дасть більшу економію, ніж `delay(...)`, бо CPU
  зупиняється, а RAM і контекст програми зберігаються.
- Вмикати light sleep без продуманої BLE-кнопки зараз ризиковано: користувач
  може втратити простий шлях до налаштування Wi-Fi/API через телефон.
- Для watering hub sleep має бути прив'язаний до найближчої події, а не до
  фіксованої паузи, інакше можна запізнитися із закриттям клапана, перевіркою
  аварійного потоку або наступним читанням датчика.

## Очікуваний ефект

Значна економія CPU-частини, особливо коли Wi-Fi/BLE вже обмежені.
